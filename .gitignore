# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local secrets
.env

# Next.js
/.next
/out
next-env.d.ts
*.tsbuildinfo

# Legacy Docusaurus tree, staged for deletion
/.legacy-docusaurus

# Static export output
/out

# Transient build state (signed URLs that expire within the hour)
/.cache

# Archive recovered from the previous host. Kept locally for reference, not
# committed -- see README.md.
/super-salvage
